/** @type {import('next').NextConfig} */
const nextConfig = {
  // Deployed to Vercel, which runs Next.js natively: no static export, so
  // route handlers, server components and next/image optimization all work.
  reactStrictMode: true,
};

export default nextConfig;
